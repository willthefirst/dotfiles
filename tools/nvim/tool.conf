# tools/nvim/tool.conf

# Layer sources
layers_base="local:configs/nvim"
layers_stripe="STRIPE_DOTFILES:nvim"

# Target location
target="${XDG_CONFIG_HOME:-$HOME/.config}/nvim"

# Merge hook - custom Lua for Neovim's complex structure
merge_hook="./merge.sh"
