# tools/ghostty/tool.conf

# Layer sources (base only, no work-specific ghostty config)
layers_base="local:configs/ghostty"

# Target location
target="${XDG_CONFIG_HOME:-$HOME/.config}/ghostty"

# Merge hook - symlink the directory
merge_hook="builtin:symlink"
