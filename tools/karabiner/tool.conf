# tools/karabiner/tool.conf

# Layer sources (base only, no work-specific karabiner config)
layers_base="local:configs/karabiner"

# Target location
target="${XDG_CONFIG_HOME:-$HOME/.config}/karabiner"

# Merge hook - symlink the directory
merge_hook="builtin:symlink"
