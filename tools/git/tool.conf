# tools/git/tool.conf

# Layer sources
layers_base="local:configs/git"
layers_stripe="STRIPE_DOTFILES:git"

# Target location
target="${HOME}/.gitconfig"

# Merge hook - use git's native [include] directive
merge_hook="./merge.sh"
