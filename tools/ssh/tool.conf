# tools/ssh/tool.conf

# Layer sources
layers_base="local:configs/ssh"
layers_stripe="STRIPE_DOTFILES:ssh"

# Target location
target="${HOME}/.ssh/config"

# Merge hook - concatenate all layers
merge_hook="builtin:concat"
